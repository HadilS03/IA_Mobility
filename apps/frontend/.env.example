# URL de l'API du service IA (service Python Flask).
# En développement local (frontend et API sur la même machine) :
VITE_API_URL=http://localhost:5000

# Clé d'API transmise aux endpoints de données (doit correspondre au
# DATA_API_KEY du service Python).
VITE_DATA_API_KEY=change-me
